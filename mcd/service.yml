kind: Service
apiVersion: v1
metadata:
  name: mcd-deployment
  labels: 
    app: mcd
spec:
  type: NodePort
  selector:
    app: mcd-deployment
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  - protocol: TCP
    port: 30
    targetPort: 30